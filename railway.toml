[build]
builder = "dockerfile"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[services.web]
tcpProxyApplicationPort = 80

[services.db]
tcpProxyApplicationPort = 3306